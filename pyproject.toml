[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dcs"
version = "0.1.0"
description = "Deterministic Compiler System (DCS) — structured REQ to verified artifacts with reproducible proof"
authors = [{name = "Justin Wilsey"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyyaml",
    "sqlalchemy",
    "pydantic",
    "httpx",
    "beautifulsoup4",
    "lxml",
    "markdownify",
    "fastapi",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "black",
    "isort",
    "flake8",
]

[tool.black]
line-length = 88
target-version = ['py38']

[tool.isort]
profile = "black"
line_length = 88
